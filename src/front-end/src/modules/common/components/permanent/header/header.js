import {AuthRoutes} from "../../../../routes/auth-routes";

export const Header = () => {
    return (
            <header>
                <AuthRoutes/>
                header
            </header>
    )
}
